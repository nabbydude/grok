import { Pattern, r } from "@/r";

import { StaticAbility } from "@/types/ability";

// import { parseState } from "@/branches/state/_";

// export const parseStaticAbility = r`${parseState}.`.as(
//   ([state]) => <StaticAbility>{ type: "static", state }
// );
